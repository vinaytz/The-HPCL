// app/(dealer)/_layout.tsx
import { Stack } from "expo-router";

export default function DealerLayout() {
  return <Stack screenOptions={{}} />;
}

export const path = "dealer"; // now /dealer/dashboard
